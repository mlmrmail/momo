// this module will create the required DBs if not existing

function initDb() {

    // check if customers collection exists or not
    
    // create collection of customers

    // create collection of addresses


  return '';
}


module.exports = {
  initDb,
};
